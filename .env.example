# Copy to .env. Never commit .env.
# Pick ONE provider to start; Strands supports several model providers.
OPENROUTER_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
# Default model for development (cheap/free). Override per run with --model.
FAULTPROP_MODEL=
